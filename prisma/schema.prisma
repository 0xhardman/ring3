generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Ring3Record {
  gifterAdd    String @id @default(cuid())
  recipientAdd String
  gifterSig    String
  recipientSig String
}
